parent_cfg: configs/credsplatting/dtu_pretrain_vggt.yaml

train_dataset_module: lib.datasets.nerf.credsplatting_vggt
test_dataset_module: lib.datasets.nerf.credsplatting_vggt

credsplatting:
    reweighting: True
    scale_factor: 200
    cas_config:
        render_if: [False, True]

train_dataset:
    data_root: './datasets/nerf'
    input_h_w: [800, 800]
    split: 'train'
    input_ratio: 1.

test_dataset:
    data_root: './datasets/nerf'
    input_h_w: [800, 800]
    split: 'test'
    input_ratio: 1.
